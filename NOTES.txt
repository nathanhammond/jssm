TODO:
- Create example site.
- Write unit tests.
- Find a solution to the "repetition of history" problem for manually re-entered hashes.
- Add queueing in the case of all transition functions.

NEW FEATURES:
- Add a helper function to load in CSS, but only if it isn't already loaded.
- Add a helper function to load in JavaScript synchronously.
- Allow for storing data with history states.
- Allow for updating the page title.
- Allow for custom callbacks per hash change.

STANDALONE VERSION:
- Add fully-featured browser detection.
- Add an event attaching mechanism for ready, load, and hashchange.
- Add a basic object extend function.
- Add a mechanism to streamline calls to setHash.